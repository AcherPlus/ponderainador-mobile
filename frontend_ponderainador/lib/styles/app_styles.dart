import 'package:flutter/material.dart';

class AppStyles {
  // Color Blanco
  static const Color kWhiteColor = Colors.white;

  // Color Morado (para indicador)
  static const Color indicatorColor = Color.fromRGBO(134, 134, 161, 1.0);
}
